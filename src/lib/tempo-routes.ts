import { RouteObject } from "react-router-dom";

// This file provides routes for Tempo storyboards
// It's empty by default but will be populated by Tempo at runtime
const routes: RouteObject[] = [];

export default routes;
